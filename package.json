{
  "name": "bulbasaur",
  "version": "1.0.0",
  "description": "Binance exchange trading and alerting bot",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rimraf dist/ && babel ./ --out-dir dist/ --ignore ./node_modules,./.babelrc,./package.json,./npm-debug.log --copy-files",
    "start": "npm run build && node dist/app.js"
  },
  "author": "Jeremy Wang",
  "license": "MIT",
  "dependencies": {
    "binance-api-node": "^0.4.3",
    "body-parser": "^1.18.2",
    "bootbot": "^1.0.14",
    "express": "^4.16.2",
    "plotly": "^1.0.6",
    "rxjs": "^5.5.6"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1",
    "rimraf": "^2.6.2"
  }
}
